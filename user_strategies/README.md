Place Python strategy modules in this directory and press **Reload plugins** in the dashboard.

Each module should export a `StrategyPlugin` subclass as `STRATEGY`. See `example_strategy.py` and `docs/STRATEGY_API.md`.
